#include <stdio.h>

/**
 * main - prints length of string
 * Return: length of string
 */
int main(void)
{
	int i;
	char s[] = "Programming is fun";

	for (i = 0; s[i] != '\0'; ++i)

	printf("Length of the string: %d", i);
	return (0);
}

